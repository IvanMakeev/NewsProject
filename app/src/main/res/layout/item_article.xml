<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="@dimen/default_dp"
        android:padding="@dimen/default_dp"
        app:cardCornerRadius="@dimen/default_dp"
        app:cardUseCompatPadding="true"
        android:focusable="true"
        android:clickable="true"
        android:foreground="?android:attr/selectableItemBackground">

    <LinearLayout android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:orientation="vertical">

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/title"
                android:layout_gravity="center"
                android:textAppearance="@style/TextAppearance.AppCompat.Large"
                tools:text="Title"/>

        <ProgressBar android:layout_width="wrap_content"
                     android:layout_height="wrap_content"
                     android:layout_gravity="center"
                     android:id="@+id/loading_image_progress"
                     android:visibility="visible"/>

        <ImageView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/article_image"
                android:layout_gravity="center"
                android:contentDescription="@string/picture"
                android:paddingStart="@dimen/default_dp"
                android:paddingLeft="@dimen/default_dp"
                android:paddingTop="@dimen/default_dp"
                android:paddingEnd="@dimen/default_dp"
                android:paddingRight="@dimen/default_dp"/>

        <TextView
                android:id="@+id/description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:padding="@dimen/default_dp"
                android:textAppearance="@style/TextAppearance.AppCompat.Medium"
                tools:text="description"/>

        <TextView
                android:id="@+id/date"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="@dimen/default_dp"
                android:textAppearance="@style/TextAppearance.AppCompat.Small"
                tools:text="date"/>
    </LinearLayout>

</androidx.cardview.widget.CardView>